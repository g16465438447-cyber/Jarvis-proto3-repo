# System Pilot environment contract (Phase 2: Link)

# Choose one provider for API-backed routing, or leave blank for local/offline mode.
LLM_PROVIDER=

# Optional provider credentials (set only what you use).
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GOOGLE_API_KEY=

# Optional runtime defaults.
PILOT_DEFAULT_DRY_RUN=true
PILOT_LOG_LEVEL=info

