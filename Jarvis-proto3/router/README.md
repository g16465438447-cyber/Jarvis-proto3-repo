# Router Layer

Implementation lives in `src/router/`.

Current module:
- `src/router/router.ts`
  - Parses constrained request templates.
  - Emits schema-validated Plan JSON only.
  - Never executes tools.

