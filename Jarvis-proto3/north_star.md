# North Star

Build a local desktop "System Pilot" app that:
- understands plain-English task requests,
- generates safe deterministic plans,
- executes approved steps on the local machine (files, apps, browser),
- shows live execution state, writes full run logs, and self-repairs common failures.

Primary value:
- "Do X on my computer" should complete reliably with minimal babysitting.

